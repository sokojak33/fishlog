export type FishCatch = {
  id: number;
  species: string;
  location: string;
  date: string;
  length: number;
};

export type NewFishCatch = {
  species: string;
  location: string;
  date: string;
  length: number;
};
